function rx = channel(sx, len*)
    % add AWGN + modeling error (?)
    p = sum(sx.^2)/(len*sample_size);
    snr = 20;            % dB
    snr_lim = 10^(snr/10);
    const = sqrt(p)/sqrt(snr_lim);
    noise = const*randn(1, len*sample_size);
    sx = sx + noise;
end